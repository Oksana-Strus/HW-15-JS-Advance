import { AlphabetOrderPipe } from './alphabet-order.pipe';

describe('AlphabetOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabetOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
